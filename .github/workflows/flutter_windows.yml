name: Build Flutter Windows VPN

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- بخش ستاپ SSH (اگر ریپوی پرایوت دارید) ---
      # - name: Setup SSH Agent
      #   uses: webfactory/ssh-agent@v0.9.0
      #   with:
      #     ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      # ---------------------------------------------

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'
          architecture: x64
          cache: true

      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop

      - name: Install dependencies
        run: flutter pub get

      - name: Build Windows (Release)
        run: flutter build windows --release

      # اصلاح شده: استفاده از اکشن معتبر برای نصب Inno Setup
      - name: Setup Inno Setup
        run: choco install innosetup --yes

      # ساخت فایل نصبی با استفاده از اسکریپتی که ساختید
      - name: Compile Installer
        run: iscc installer.iss

      # آپلود فایل نهایی
      - name: Upload Installer Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ParsiVPN-Installer
          path: ParsiVPN_Setup.exe
